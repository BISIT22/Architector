# Экранные формы и электронные документы AI Architect

**Автор:** Алексей Марышев  
**Проект:** AI-архитектор для генерации инструкций 3D-моделирования  

---

## 2. Экранные формы приложения

### 2.1 Главная форма (Main Dashboard)

**Название:** Главная страница AI Architect  
**Функция:** Центральный узел приложения, обеспечивающий навигацию между основными функциями системы и отображение логотипа с общей информацией о назначении приложения.

**Элементы формы:**
- Логотип системы "🏗️ AI Architect"
- Описательный текст: "Ваш персональный помощник для генерации архитектурных концепций"
- Боковое меню навигации (Sidebar) с выбором разделов
- Разделительная линия для визуального оформления
- Область для отображения выбранного раздела

**Доступ:** Все пользователи системы (в данной реализации авторизация не предусмотрена - система предназначена для индивидуального использования)

### 2.2 Форма генерации инструкций (Generation Form)

**Название:** "Генерация Инструкций" - Новая Архитектурная Концепция  
**Функция:** Основная рабочая форма системы, предназначенная для ввода пользовательского запроса, обработки его через AI-модель и генерации структурированных инструкций для 3D-моделирования.

**Элементы формы:**

*Левая панель - "Параметры Запроса":*
- Многострочное текстовое поле "Опишите вашу идею" (высота 200px)
- Однострочное поле "Архитектурный стиль" 
- Однострочное поле "Основные материалы"
- Кнопка "✨ Сгенерировать Инструкции" (стилизованная с анимацией)

*Правая панель - "Результат Генерации (JSON)":*
- Область отображения JSON-результата с подсветкой синтаксиса
- Кнопка "Очистить результат" (появляется только при наличии результата)
- Информационное сообщение-подсказка (при отсутствии результата)

*Интерактивные элементы:*
- Спиннер с текстом "Искусственный интеллект творит магию... 🧙‍♂️"
- Анимация "воздушные шарики" при успешной генерации
- Toast-уведомления о сохранении в базу данных

**Доступ:** Все пользователи системы

### 2.3 Форма просмотра базы данных (Database View)

**Название:** "Просмотр Базы Данных" - Архив Сгенерированных Концепций  
**Функция:** Форма для просмотра, анализа и управления историей сгенерированных архитектурных концепций с возможностью детального изучения каждой записи.

**Элементы формы:**
- Заголовок раздела
- Динамический список раскрывающихся блоков (expander), каждый содержит:
  - Заголовок с кратким названием концепции и датой создания
  - Секция "Исходный запрос" с полным текстом пользовательского ввода
  - Секция "Сгенерированные инструкции (JSON)" с форматированным JSON-выводом
- Информационные сообщения:
  - Сообщение о пустой базе данных (если записей нет)
  - Сообщения об ошибках при загрузке данных

**Доступ:** Все пользователи системы

---

## 3. Электронные документы системы

### 3.1 JSON-документ с архитектурными инструкциями

**Название:** Structured Architecture Instructions Document  
**Формат:** JSON  
**Назначение:** Основной выходной документ системы, содержащий структурированные инструкции для создания 3D-модели архитектурного объекта.

**Структура документа:**
```json
{
  "object_type": "строка - тип объекта (здание/мост/башня/забор)",
  "style": "строка - архитектурный стиль",
  "components": [
    {
      "name": "строка - название компонента",
      "type": "строка - тип 3D примитива (cube/cylinder/sphere/plane/cone/torus)",
      "position": [число, число, число] - координаты X,Y,Z,
      "scale": [число, число, число] - масштаб по осям,
      "rotation": [число, число, число] - поворот в радианах,
      "material": "строка - предполагаемый материал"
    }
  ],
  "modifiers": [
    {
      "component": "строка - название компонента для применения",
      "type": "строка - тип модификатора",
      "parameters": {} - объект с параметрами модификатора
    }
  ]
}
```

**Создается:** Автоматически при каждом успешном запросе через AI-модель  
**Сохраняется:** В базе данных SQLite в поле `instructions` таблицы `generated_instructions`  
**Используется:** Как техническое задание для 3D-моделирования, может быть экспортирован для использования в Blender, 3ds Max, Maya и других 3D-пакетах

### 3.2 База данных концепций (SQLite Database)

**Название:** architect.db  
**Формат:** SQLite database file  
**Назначение:** Централизованное хранилище всех сгенерированных архитектурных концепций с возможностью поиска и анализа истории работы.

**Структура данных:**
- **Таблица:** `generated_instructions`
- **Поля:**
  - `id` (INTEGER, PRIMARY KEY) - уникальный идентификатор записи
  - `name` (STRING) - краткое название концепции (первые 50 символов запроса)
  - `input_prompt` (TEXT) - полный исходный текстовый запрос пользователя
  - `instructions` (JSON) - сгенерированный JSON-документ с инструкциями
  - `created_at` (DATETIME) - timestamp создания записи

**Создается:** Автоматически при первом запуске приложения (если не существует)  
**Обновляется:** При каждой успешной генерации новой концепции  
**Используется:** Для ведения истории работы, анализа запросов, повторного использования успешных концепций

### 3.3 Конфигурационный документ (.env)

**Название:** .env  
**Формат:** Environment variables file  
**Назначение:** Хранение конфигурационных параметров системы и подключений к внешним сервисам.

**Содержимое:**
```
# Конфигурация Ollama
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=gemma3:4b

# Конфигурация базы данных
DATABASE_URL=sqlite:///data/architect.db

# Другие настройки
LOG_LEVEL=INFO
DEBUG=False
MAX_RETRIES=3
TIMEOUT=60
```

**Создается:** Вручную при развертывании системы  
**Используется:** При инициализации всех подсистем приложения  
**Модифицируется:** Администратором системы для настройки параметров работы

### 3.4 Логи системы (System Logs)

**Название:** Системные логи приложения  
**Формат:** Текстовые файлы / консольный вывод  
**Назначение:** Отслеживание работы системы, диагностика ошибок и мониторинг производительности.

**Типы логов:**
- Логи инициализации AI-модели
- Логи запросов к Ollama API
- Логи операций с базой данных
- Логи обработки пользовательских запросов
- Логи ошибок и исключений

**Создается:** Автоматически во время работы приложения  
**Используется:** Для диагностики проблем, мониторинга производительности и анализа использования системы

---

## 4. Пользовательские сценарии работы с формами

### 4.1 Сценарий создания новой архитектурной концепции:
1. Пользователь открывает главную форму
2. Выбирает "Генерация Инструкций" в боковом меню
3. Заполняет текстовое описание архитектурного объекта
4. Опционально указывает стиль и материалы
5. Нажимает кнопку "Сгенерировать Инструкции"
6. Наблюдает анимацию загрузки
7. Получает JSON-результат в правой панели
8. Система автоматически сохраняет результат в базу данных

### 4.2 Сценарий просмотра истории работы:
1. Пользователь выбирает "Просмотр Базы Данных" в боковом меню
2. Видит список всех сгенерированных концепций
3. Раскрывает интересующую запись
4. Изучает исходный запрос и полученные инструкции
5. При необходимости использует данные для повторного моделирования

---

## 5. Техническое обоснование выбора форм

**Минималистичный дизайн:** Выбрана двухэкранная архитектура для фокусировки внимания на ключевых функциях - генерации и просмотре истории.

**Единая форма ввода:** Все параметры запроса объединены на одном экране для упрощения процесса создания концепции.

**Разделение ввода и вывода:** Четкое визуальное разделение области ввода данных и отображения результатов повышает usability.

**Встроенная навигация:** Боковое меню обеспечивает быстрое переключение между функциями без потери контекста.

**Responsive design:** Интерфейс автоматически адаптируется к размеру окна браузера благодаря использованию Streamlit framework.