# Работа с сохранением данных в базе данных

## Важно! База данных сохраняется между запусками

Система AI-Architect использует локальную базу данных SQLite, которая **автоматически сохраняет все данные** между запусками приложения.

## Расположение базы данных

База данных хранится в файле:
```
architector/data/architect.db
```

Этот файл содержит всю историю ваших запросов, сгенерированные инструкции, обратную связь и статистику.

## Автоматическое поведение

### При запуске приложения (streamlit run src/web/app.py):

1. **Если БД существует** - приложение подключается к существующей БД и загружает все данные
2. **Если БД не существует** - автоматически создается новая пустая БД
3. **Если в БД не хватает таблиц** - недостающие таблицы создаются автоматически

### Сохранение данных:

- ✅ Все запросы сохраняются автоматически
- ✅ История сохраняется между сессиями
- ✅ Избранное сохраняется постоянно
- ✅ Статистика накапливается со временем

## Управление базой данных

### Обычная инициализация (сохраняет данные):
```bash
python scripts/init_database.py
```
Этот скрипт:
- Создает БД если её нет
- НЕ удаляет существующую БД
- Добавляет недостающие таблицы если нужно

### Полная переинициализация (удаляет все данные):
```bash
python scripts/init_database.py --recreate
```
⚠️ **ВНИМАНИЕ**: Флаг `--recreate` полностью удаляет существующую БД!

### Добавление тестовых данных:
При запуске `init_database.py` вам будет предложено добавить тестовые данные. Это полезно для демонстрации функциональности.

## Резервное копирование

### Создание резервной копии:
```bash
copy data\architect.db data\architect_backup.db
```

### Восстановление из резервной копии:
```bash
copy data\architect_backup.db data\architect.db
```

## Просмотр данных

### Через веб-интерфейс:
1. Запустите приложение: `streamlit run src/web/app.py`
2. Используйте разделы:
   - 📊 История запросов - все ваши запросы
   - 📈 Аналитика - статистика использования
   - ⭐ Избранное - сохраненные результаты

### Через Python:
```python
from src.database.connection import get_db
from src.database.operations import DatabaseOperations

db = next(get_db())
db_ops = DatabaseOperations(db)

# Получить все запросы
requests = db_ops.get_all_requests()
for req in requests:
    print(f"ID: {req.id}, Prompt: {req.input_prompt[:50]}...")

# Получить статистику
stats = db_ops.get_statistics(days=30)
print(f"Всего запросов: {stats['total_requests']}")
```

### Через SQLite браузер:
Вы можете открыть файл `data/architect.db` в любом SQLite браузере (например, DB Browser for SQLite) для прямого просмотра данных.

## Очистка данных

### Удаление отдельных записей:
Используйте веб-интерфейс - у каждого запроса есть кнопка "Удалить"

### Полная очистка БД:
```bash
# Вариант 1: Удаление файла
del data\architect.db

# Вариант 2: Пересоздание через скрипт
python scripts\init_database.py --recreate
```

## Миграция данных

### Экспорт в JSON:
```python
import json
from src.database.connection import get_db
from src.database.operations import DatabaseOperations

db = next(get_db())
db_ops = DatabaseOperations(db)

# Экспорт всех запросов
requests = db_ops.get_all_requests(limit=1000)
data = [
    {
        "id": req.id,
        "prompt": req.input_prompt,
        "style": req.style,
        "instructions": req.generated_instructions,
        "created_at": req.created_at.isoformat() if req.created_at else None
    }
    for req in requests
]

with open("backup.json", "w", encoding="utf-8") as f:
    json.dump(data, f, ensure_ascii=False, indent=2)
```

## Размер базы данных

- Новая пустая БД: ~57 KB
- С 100 запросами: ~200 KB
- С 1000 запросами: ~2 MB

SQLite эффективно хранит данные, поэтому БД остается компактной даже при большом количестве записей.

## Часто задаваемые вопросы

**Q: Почему мои данные исчезли после перезапуска?**
A: Проверьте, что вы не используете флаг `--recreate` при инициализации БД.

**Q: Как перенести БД на другой компьютер?**
A: Просто скопируйте файл `data/architect.db` на новый компьютер в ту же директорию.

**Q: Можно ли использовать PostgreSQL вместо SQLite?**
A: Да, измените `DATABASE_URL` в файле `.env` и установите драйвер PostgreSQL.

**Q: Как узнать, сколько записей в моей БД?**
A: Запустите `python scripts/init_database.py` - он покажет статистику БД.