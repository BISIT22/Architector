# Расширенные возможности работы с базой данных

## Обзор новых функций

Система AI-Architect теперь представляет собой полноценную информационную систему со следующими возможностями:

### 📊 История запросов
- Просмотр всех выполненных запросов
- Фильтрация по статусу (завершенные, в обработке, с ошибкой)
- Фильтрация по сессии пользователя
- Детальная информация о каждом запросе
- Возможность удаления и добавления в избранное

### 📈 Аналитика
- Статистика за выбранный период (7, 30, 90 дней)
- Метрики: общее количество запросов, процент успешности, среднее время обработки
- Графики распределения по статусам и типам запросов
- Топ популярных архитектурных стилей
- Последняя активность за 24 часа

### ⭐ Избранное
- Сохранение понравившихся результатов
- Быстрый доступ к лучшим концепциям
- Возможность добавления тегов для категоризации

### 🔍 Поиск
- Полнотекстовый поиск по запросам
- Поиск по текстовому описанию и стилю
- Быстрый доступ к результатам

### 📝 Обратная связь
- Оценка качества сгенерированных инструкций
- Рейтинг от 1 до 5 звезд
- Комментарии для улучшения системы
- Отметка полезности результата

## Структура базы данных

### Основные таблицы:

1. **generation_requests** - Запросы на генерацию
   - Статус обработки (pending, processing, completed, failed)
   - Входные параметры (промпт, стиль, материалы)
   - Результаты генерации
   - Время обработки и ошибки

2. **generated_instructions** - Сгенерированные инструкции
   - Связь с запросом
   - Флаг избранного
   - Теги для категоризации

3. **user_feedback** - Обратная связь пользователей
   - Рейтинг и комментарии
   - Отметка полезности

4. **refinement_history** - История улучшений
   - Итерации улучшения инструкций
   - Сохранение предыдущих версий

5. **system_stats** - Системная статистика
   - Ежедневные метрики использования
   - Агрегированная статистика

## Использование

### Инициализация базы данных

```bash
python scripts/init_database.py
```

### Запуск приложения

```bash
streamlit run src/web/app.py
```

### Программный доступ к БД

```python
from src.database.connection import get_db
from src.database.operations import DatabaseOperations

# Получение сессии БД
db = next(get_db())
db_ops = DatabaseOperations(db)

# Создание запроса
request = db_ops.create_generation_request(
    input_prompt="Современный дом в стиле минимализм",
    style="Минимализм",
    materials=["бетон", "стекло"]
)

# Получение статистики
stats = db_ops.get_statistics(days=30)
print(f"Всего запросов: {stats['total_requests']}")
print(f"Успешность: {stats['successful_rate']}%")

# Поиск запросов
results = db_ops.search_requests("минимализм")

# Добавление обратной связи
feedback = db_ops.add_feedback(
    request_id=request.id,
    rating=5,
    comment="Отличный результат!",
    is_useful=True
)
```

## API операций с базой данных

### CRUD операции

- `create_generation_request()` - Создание нового запроса
- `update_request_status()` - Обновление статуса запроса
- `get_request_by_id()` - Получение запроса по ID
- `get_all_requests()` - Получение списка запросов с фильтрацией
- `delete_request()` - Удаление запроса

### Работа с инструкциями

- `create_instruction_from_request()` - Создание инструкции из запроса
- `get_all_instructions()` - Получение списка инструкций
- `toggle_favorite()` - Переключение флага избранного

### Обратная связь

- `add_feedback()` - Добавление обратной связи
- `get_request_feedback()` - Получение обратной связи для запроса

### Аналитика

- `get_statistics()` - Получение статистики за период
- `get_popular_styles()` - Получение популярных стилей
- `get_recent_activity()` - Получение последней активности
- `search_requests()` - Поиск запросов по тексту
- `update_system_stats()` - Обновление системной статистики

## Преимущества новой архитектуры

1. **Масштабируемость** - Нормализованная структура БД позволяет легко расширять функционал
2. **Отслеживаемость** - Полная история всех операций и запросов
3. **Аналитика** - Возможность анализа использования и улучшения системы
4. **Обратная связь** - Механизм сбора отзывов для улучшения качества
5. **Производительность** - Индексы и оптимизированные запросы для быстрой работы

## Миграция на PostgreSQL

При необходимости система легко мигрирует на PostgreSQL:

1. Измените `DATABASE_URL` в `.env`:
```
DATABASE_URL=postgresql://user:password@localhost/architect_db
```

2. Установите драйвер PostgreSQL:
```bash
pip install psycopg2-binary
```

3. Запустите инициализацию БД:
```bash
python scripts/init_database.py
```

Все модели и операции полностью совместимы с PostgreSQL благодаря использованию SQLAlchemy ORM.